{
  "name": "putitoutthere-docs",
  "version": "0.0.0",
  "private": true,
  "description": "Put It Out There documentation site (VitePress).",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview --port 4173",
    "test:unit": "vitest run",
    "test:integration": "vitepress build && playwright test",
    "test:integration:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "vitepress": "^1.5.0",
    "vitest": "^2.1.0"
  }
}
